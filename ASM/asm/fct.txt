#include<iostream>
using namespace std;
unsigned a;

void dif(int x,int y, int z)
{
	_asm
	{
		mov eax, [ebp+8];
		sub eax, [ebp+12];
		sub eax, [ebp+16];
	}

}


int main()
{
	_asm
	{
		push dword ptr 3 //z
		push dword ptr 5 //y
		push dword ptr 19 //x
		call dif
		mov a,eax
		add esp,12

	}
	cout<<a;
}