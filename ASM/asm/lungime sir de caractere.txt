#include<iostream>
using namespace std;


int lungime(char *s)
{
	_asm
	{
		mov eax, 0
		mov ebx, [ebp+8]

bucla:
		cmp [eax+ebx],0
		je stop
		inc eax
		jmp bucla
stop:
	}
}

int main()
{
	int l;
	char *sir="m-amsaturat";
	_asm
	{
		push sir
		call lungime
		add esp,4
		mov l,eax
	}
	cout<<l;
}